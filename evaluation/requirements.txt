# Extra dependencies for the end-to-end evaluation framework.
# The base app already pins mlflow>=3,<4; the multi-turn simulator + GenAI
# scorers used here need 3.10+. (The `aiss` conda env already has 3.14.)
mlflow>=3.10,<4
openai>=2.0.0
python-dotenv>=1.0.0
# tqdm gives the simulator a progress bar (optional but nice).
tqdm>=4.66
