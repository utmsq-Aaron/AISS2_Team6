requests>=2.31.0
python-dotenv>=1.0.0
openai>=2.0.0
mcp>=1.0.0
anyio>=4.0              # mcp 1.x client uses anyio subscript syntax unavailable in 3.x
streamlit>=1.35.0
plotly>=5.20.0
statsmodels>=0.14.0        # required by plotly express trendline="ols"
pandas>=2.0.0
folium>=0.17.0
streamlit-folium>=0.22.0
polyline>=2.0.0
staticmap>=0.5.7           # static route-map PNGs for the Telegram bridge (no browser)
garminconnect>=0.3.0
telethon>=1.42.0          # Settings-tab Telegram session-string generation
playwright>=1.44.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
starlette                 # telegram_mcp proxy ASGI app (also a transitive mcp dep)
faster-whisper>=1.0.0     # local voice-memo transcription (portable CPU engine, no ffmpeg needed)
# Apple Silicon GPU speedup (optional, Mac-only — also needs `brew install ffmpeg`):
#   pip install mlx-whisper
aiohttp>=3.9.0
psutil>=5.9.0              # cross-platform port/process management for MCP server lifecycle
kaleido>=0.2.1             # static Plotly PNG export (fig.to_image) for Telegram chart delivery
psutil

# ── Agent layer: LangGraph specialists + A2A coordination ─────────────────────
# Orchestrator + 4 specialists are LangGraph ReAct agents, each its own A2A server.
# a2a-sdk is pinned to the 0.3.x (pydantic) line to match the official Python
# tutorial / A2A examples; create_agent comes from langchain 1.x.
a2a-sdk>=0.3.26,<0.4
langgraph>=1.2,<2
langchain>=1,<2
langchain-core>=1.4,<2
langchain-openai>=1.3,<2     # ChatOpenAI against the OpenAI-compatible KIT gateway
langchain-google-genai>=2,<4 # optional: LLM_PROVIDER=gemini (Google Gemini)
mlflow>=3,<4                 # tracing the agent layer (LangGraph + LLM + tool spans); UI on :5000
