requests>=2.31.0
python-dotenv>=1.0.0
openai>=2.0.0
mcp>=1.9,<2               # 1.x API: mcp.server.fastmcp, inputSchema, isError. 2.x breaks all servers.
anyio>=4.0                # async runtime shared by mcp/starlette — pinned as a floor
plotly>=5.20.0
pandas>=2.0.0
numpy>=1.26               # imported directly by core/fitness_rag.py and core/user_memory.py
polyline>=2.0.0
staticmap>=0.5.7          # static route-map PNGs for the Telegram bridge
matplotlib>=3.8           # core/viz_telegram.py — the Telegram chart renderer
garminconnect>=0.3.0
telethon>=1.42.0
qrcode>=7,<9              # optional: telegram_bridge.py --login-qr
playwright>=1.44.0        # optional: server-side flythrough MP4 render
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # FastAPI UploadFile (avatar upload)
starlette
faster-whisper>=1.0.0     # local voice-memo transcription (mlx-whisper is picked up
                          # instead when present on Apple Silicon — install it yourself)
kaleido>=0.2.1            # Plotly PNG export for Telegram

# Agent layer: LangGraph specialists + A2A coordination
a2a-sdk>=0.3.26,<0.4
langgraph>=1.2,<2
langchain>=1,<2
langchain-core>=1.4,<2
langchain-openai>=1.3,<2
langchain-google-genai>=2,<4  # optional: LLM_PROVIDER=gemini
mlflow>=3,<4

# Fitness Expert agent: local RAG (embeddings run locally, no external API)
sentence-transformers>=3,<6

# Only needed to REBUILD the RAG corpus from the source PDFs
# (python -m scripts.extract_literature_corpus). The shipped corpus text and the
# index built from it need none of this — see data/fitness_library/SOURCES.txt.
PyMuPDF>=1.24
